# ResolveFlow 实施约定

## 当前状态与目标

框架版本1.1，2026-09-15。本文档包是实施依据；业务代码、运行命令和指标尚未验证。用户目标是高质量简历项目，覆盖Java微服务和Agent工程，使用合成业务数据、模拟资金通道、真实模型效果评测。

## 开始工作

1. 读 README.md、docs/implementation-handoff.md、docs/dsh-migration-handoff.md、tasks/plan.md、tasks/todo.md。
2. 只读取当前任务相关规格和契约；不要仅根据对话记忆实施。
3. 查看工作区已有代码、进度与测试结果，接续第一个依赖已满足的未完成任务。
4. T00锁定依赖补丁和镜像版本，T01建立命令入口；此前不要假定文档中的命令已存在。

## 规范权威

业务规则以 docs/product-spec.md 为准，数据不变量与状态以 docs/domain-model.md 为准，协议以 docs/contracts.md 和 contracts/*.schema.json 为准，其他设计见专题文件。有实质冲突先记录并解决，不能任选一个实现。

Agent运行机制以docs/agent-harness.md为主线，结合docs/agent-spec.md、docs/context-engineering.md和docs/trajectory-replay.md实施。T35–T39是核心工作包，不是可跳过的附加演示；编号不代表执行顺序，须先满足依赖。

任务清单允许拆成更小步骤；固定服务边界、业务范围、风险授权、资金不变量、评测隔离不可静默改变。框架已授权的表、依赖、测试和CI建设可正常推进，无须逐项重新询问。

## 不变量

- Agent身份不能退款、补发、审批、修改政策或枚举其他用户订单。
- 退款与补发共享commerce的订单行权益占用，同一行最多一次成功补救。
- 网络超时不能当作退款失败；未知结果保持占用，查询对账后才终结。
- Java拥有工单及审批状态；Python拥有运行状态，两者不相互覆盖。
- Redis锁、MQ顺序和模型自报置信度都不是资金正确性依据。
- Outbox按至少一次投递设计；消费落库、inbox去重和本地事件必须在同一事务。
- 只有数据库已持久化的LLM结果才能保证复用；不能承诺故障下零重复模型调用。
- 模型根据Observation决定下一业务行动；Harness控制边界，不按case_type硬编码完整调查路线。
- 每次模型调用保留实际ContextPacket与manifest；同run恢复不静默换Prompt、上下文策略或预算。
- 原始证据与模型推断分离；裁剪不能修改权威关键字段，冲突不可被摘要抹平。
- 回放隔离生产身份与写入能力；历史未覆盖分支必须REPLAY_MISS，不回源生产或伪造工具结果。
- 指标必须链接实际报告；模拟负载、合成样本、真实模型与mock模式分开标记。

## 实施方式

- 按垂直切片交付；一个任务过大时在tasks/todo.md补子任务，通常一次改3–5个核心文件。
- 先写关键行为失败测试，再实现；不要用与实现同构的断言替代不变量验证。
- 每个完成项记录日期、文件、实际命令、结果和报告路径；没运行的验证写“未运行”及原因。
- 提交前完成相关格式、单元、契约、集成检查；没有新的风险不要反复跑无关重测试。
- 保留用户已有改动；禁止把工作区或数据库重置成空白以绕过问题。
- 不提交密钥、客户数据或伪造报告；不把文档描述当成已完成代码。
- schema变化同步测试、生产者、消费者、迁移；数据库迁移只追加，不覆盖已执行版本。
- 不默认增加微服务、双MQ、双Agent框架、Seata、Kubernetes、Elasticsearch或多Agent。

## 阶段报告

每个里程碑输出：可演示行为、通过的测试、未通过或未执行的验证、下一步。达到门槛可继续实施；只有范围变化、权限缺失或真实架构矛盾需要用户决定。
