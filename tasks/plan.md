# 实施计划 · v1.2个人项目核心版

2026-09-18。用户已确认缩减公司级配套，以个人技术展示为目标。[核心范围](../docs/core-scope.md)是当前范围权威。T00–T02已有完成记录，保留其版本锁、骨架与契约成果；不是从零重做。

旧T03–T39不再按原顺序执行，见[历史计划](archive/v1.1-plan.md)与[历史任务记录](archive/v1.1-todo.md)。旧T35–T39的有价值部分已并入新任务，不再要求反事实/全量消融。

## 1. 交付阶段

| 阶段 | 工作包 | 验收 |
| --- | --- | --- |
| S0 范围迁移 | C00 | 核心与兼容协议分离、profile明确、现有测试不被删除 |
| S1 最小业务+提前验证Agent | C01–C03；C06可在C02后开始 | 建单/读取/审批可测，MCP和模型能力尽早冒烟 |
| S2 可靠退款 | C04–C05 | Case→MQ→Commerce→持久stub→结果闭环，重复/UNKNOWN不破账 |
| S3 核心Harness | C06–C09 | 动态决策、上下文/政策、补证/授权、预算与持久恢复 |
| S4 展示与证据 | C10–C13 | strict回放、轻量UI、真实对照、关键故障、可复现交付 |

任务编号不是排队顺序：C04可在C01后开始；C06在C02后即可先做mock/MCP，避免等所有后端配套完成才发现模型工具能力不匹配。

## 2. 精确依赖

C00→C01→C02→C03。
C01→C04；C03+C04→C05。
C02→C06；C03+C06→C07；C05+C07→C08→C09。
C09→C10；C08→C11；C09+C10+C11→C12→C13。

完整依赖以todo每项“依赖”行核对。C06选择真实模型仅做经预算确认的小样本；没有API可先做mock但不能将live标通过。外部阻塞不阻止独立任务。

## 3. 粒度与检查点

14个C编号是工作包，不是“让模型一次写14个大模块”。每项按子步骤分批，通常一次3–5个核心文件；超过5个或包含独立事务设计先进一步拆分，保留可运行结果。已有兼容代码能复用则复用。

每个阶段提交可展示行为、实际测试、未决项与下一步。关键审阅位置：C05退款事务/UNKNOWN；C09恢复/fence；C12实验分母；C13简历主张。可以继续依赖已满足任务，不重复询问已确认场景。

不再沿用240–380小时估算，也不承诺删了任务编号就节省某个百分比。先实做C00和首条闭环后再用实际投入重估，保留学习/调试/理解时间。

## 4. 核心之外

不自动启动补发/库存、反事实回放、全套RAG消融、完整观测部署或商业化功能。只有核心验收结束且用户明确选择后，才为增强能力建立新任务；不把它们放在“最后顺手完成”列表。

已有未使用资产保留但不默认部署、不伪装已实现。不清库、不清卷、不重写历史、不替换已实测主版本来追求文档整齐。
